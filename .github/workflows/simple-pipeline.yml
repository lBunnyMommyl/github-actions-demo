# .github/workflows/minimal.yml
name: Minimal CI/CD

on: [push]

jobs:
  pipeline:
    runs-on: ubuntu-latest  # Используем GitHub runners
    
    steps:
      - name: 📥 Код
        uses: actions/checkout@v4
      
      - name: 🐳 Проверка Docker
        run: |
          echo "1. Собираю тестовый образ..."
          echo "FROM alpine:3.16" > Dockerfile.test
          echo "RUN echo 'CI/CD работает!'" >> Dockerfile.test
          
          docker build -t test-image -f Dockerfile.test .
          
          echo "2. Запускаю тест..."
          docker run --rm test-image
          
          echo "✅ Все этапы пройдены!"
