# .github/workflows/no-docker.yml
name: CI/CD без Docker

on: [push]

jobs:
  ci-pipeline:
    runs-on: self-hosted
    steps:
      - name: 📥 Код
        uses: actions/checkout@v4

      - name: 1️⃣ Подготовка
        run: |
          echo "🚀 Конвейер запущен!"
          echo "Ветка: ${{ github.ref }}"
          echo "Коммит: ${{ github.sha }}"

      - name: 2️⃣ Сборка
        run: |
          echo "🏗️ Собираю проект..."
          # Ваши команды сборки
          # npm install, mvn package, pip install и т.д.
          echo "✅ Сборка завершена"

      - name: 3️⃣ Тестирование
        run: |
          echo "🧪 Запускаю тесты..."
          # Ваши команды тестирования
          # npm test, pytest, ./run-tests.sh
          echo "✅ Тесты пройдены"

      - name: 4️⃣ Деплой (демо)
        run: |
          echo "🚀 Готовлю деплой..."
          echo "Артефакты для деплоя:"
          ls -la
          echo ""
          echo "✅ Готово! Запустите деплой командой:"
          echo "./deploy.sh production"
